# 核心依赖
requests>=2.28.0
PySocks>=1.7.1
beautifulsoup4>=4.11.0
pymysql>=1.0.2
redis>=4.3.0
python-dotenv>=0.20.0

# API 服务器依赖
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# 测试依赖
pytest>=7.2.0
pytest-cov>=4.0.0

# 可选依赖（按需安装）
# playwright>=1.40.0  # 用于 JS 渲染网站，首次安装需运行: python -m playwright install chromium
# lxml>=4.9.0         # 更快的 HTML/XML 解析（可选，beautifulsoup4 后端）
